foo: myRepoConfigText
---
spring:
  config:
    activate:
      on-profile: dev
logging:
  level:
    root: debug

---

spring:
  config:
    activate:
      on-profile: test
logging:
  level:
    root: info

---

spring:
  config:
    activate:
      on-profile: product
logging:
  level:
    root: error